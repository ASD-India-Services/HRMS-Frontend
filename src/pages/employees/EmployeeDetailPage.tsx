/**
 * Wrapper component for lazy-loading the EmployeeDetail page.
 */

import { EmployeeDetail } from './EmployeeDetail';

export default function EmployeeDetailPage() {
  return <EmployeeDetail />;
}
